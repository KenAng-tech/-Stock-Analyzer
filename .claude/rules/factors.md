# Factor Model Rules

当修改 `multi_factor_model_v2.py` 或因子计算相关模块时：

## 必须遵守
1. **因子正交化** — 相关因子 > 0.7 时需正交化处理
2. **数据清洗** — NaN 值用中位数填充，异常值 3σ 截断
3. **标准化** — 因子值必须标准化 (z-score 或 min-max)
4. **复权价格** — 必须使用后复权价格计算技术指标

## 禁止
- 不要直接相加未标准化的因子值
- 不要在因子计算中引入幸存者偏差
- 不要忽略股票停牌/退市情况
